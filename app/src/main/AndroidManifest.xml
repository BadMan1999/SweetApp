<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sweetapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SweetApp">
        <activity android:name=".MainActivity2"></activity>
        <activity android:name=".Login_tenant" />
        <activity android:name=".Login_Chalet_Owner" />
        <activity android:name=".sign_up_tenant" />
        <activity android:name=".sign_up_ChaletOwner" />
        <activity android:name=".Start_project_tenant" />
        <activity android:name=".start_project_ChaletOwner" />
        <activity android:name=".Selctor_in_human" />
        <activity android:name=".SplashActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>